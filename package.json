{
  "name": "mini-agent",
  "private": true,
  "version": "0.1.0",
  "description": "Mini-Agent: TypeScript agent runtime with thin multi-language clients",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @mini-agent/server dev",
    "generate": "buf generate",
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm build && pnpm --filter @mini-agent/runtime exec node --import tsx --test src/tools/policy.test.ts && pnpm --filter @mini-agent/server exec node --import tsx --test src/paths.test.ts src/cli.smoke.test.ts && pnpm --filter @mini-agent/testkit run test",
    "test:unit": "pnpm --filter @mini-agent/testkit run test:unit",
    "test:e2e": "pnpm --filter @mini-agent/testkit run test:e2e",
    "test:live": "pnpm --filter @mini-agent/testkit run test:live",
    "testkit": "pnpm --filter @mini-agent/testkit start",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.50.0",
    "@bufbuild/protoc-gen-es": "^2.2.5",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=20"
  }
}
